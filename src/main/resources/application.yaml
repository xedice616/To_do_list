spring:
  application:
    name: to-do-list

  datasource:
    url: jdbc:postgresql://localhost:7878/todo_list_db
    username: postgres
    password: xedice2008
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

springdoc:
  api-docs:
    path: /api-docs

  swagger-ui:
    path: /swagger-ui.html